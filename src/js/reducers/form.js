import {
    
} from '../actions/appActions';

const initialState = {
    loaded: true,
};


export default function(state = initialState, action) {

    switch(action.type) {
        default :
            return state;
    }
}
